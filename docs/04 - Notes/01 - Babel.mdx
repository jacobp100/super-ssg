The default babel config provides support for JSX and TypeScript.

You can add your own `babelrc` to your project. However, for the time being, you **must** convert `import` statements to `require`s &mdash; at least until node has more stable support for ESM loaders.
